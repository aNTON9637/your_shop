{
  "scripts": {
    "build": "mkdir -p dist && cp index.html bot.js app.js package.json dist/",
    "dev": "npm run build"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.66.0"
  },
  "devDependencies": {
    "ftp-deploy": "^2.4.7"
  }
}
